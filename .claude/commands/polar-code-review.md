# Polar Code Review

Follow `.agents/commands/polar-code-review.md`. Read it and run every step.

That file is the single source of truth for this review, so it stays in sync across
Cursor, Cloud Agents, and Claude Code.
