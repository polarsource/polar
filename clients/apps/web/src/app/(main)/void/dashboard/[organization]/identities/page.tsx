import { VoidIdentitiesPage } from '@/components/Void/VoidIdentitiesPage'
import { Metadata } from 'next'

export const metadata: Metadata = { title: 'Identities' }

export default function Page() {
  return <VoidIdentitiesPage />
}
