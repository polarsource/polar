/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Properties for a benefit of type `discord`.
 */
export type BenefitDiscordProperties = {
  /**
   * The ID of the Discord server.
   */
  guildId: string;
  /**
   * The ID of the Discord role to grant.
   */
  roleId: string;
  /**
   * Whether to kick the member from the Discord server on revocation.
   */
  kickMember: boolean;
  guildToken: string;
};

/** @internal */
export const BenefitDiscordProperties$inboundSchema: z.ZodMiniType<
  BenefitDiscordProperties,
  unknown
> = z.pipe(
  z.object({
    guild_id: z.string(),
    role_id: z.string(),
    kick_member: z.boolean(),
    guild_token: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "guild_id": "guildId",
      "role_id": "roleId",
      "kick_member": "kickMember",
      "guild_token": "guildToken",
    });
  }),
);
/** @internal */
export type BenefitDiscordProperties$Outbound = {
  guild_id: string;
  role_id: string;
  kick_member: boolean;
  guild_token: string;
};

/** @internal */
export const BenefitDiscordProperties$outboundSchema: z.ZodMiniType<
  BenefitDiscordProperties$Outbound,
  BenefitDiscordProperties
> = z.pipe(
  z.object({
    guildId: z.string(),
    roleId: z.string(),
    kickMember: z.boolean(),
    guildToken: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      guildId: "guild_id",
      roleId: "role_id",
      kickMember: "kick_member",
      guildToken: "guild_token",
    });
  }),
);

export function benefitDiscordPropertiesToJSON(
  benefitDiscordProperties: BenefitDiscordProperties,
): string {
  return JSON.stringify(
    BenefitDiscordProperties$outboundSchema.parse(benefitDiscordProperties),
  );
}
export function benefitDiscordPropertiesFromJSON(
  jsonString: string,
): SafeParseResult<BenefitDiscordProperties, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BenefitDiscordProperties$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BenefitDiscordProperties' from JSON`,
  );
}
