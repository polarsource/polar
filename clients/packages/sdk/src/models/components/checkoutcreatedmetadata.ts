/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type CheckoutCreatedMetadata = {
  checkoutId: string;
  checkoutStatus: string;
  productId?: string | undefined;
};

/** @internal */
export const CheckoutCreatedMetadata$inboundSchema: z.ZodMiniType<
  CheckoutCreatedMetadata,
  unknown
> = z.pipe(
  z.object({
    checkout_id: z.string(),
    checkout_status: z.string(),
    product_id: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "checkout_id": "checkoutId",
      "checkout_status": "checkoutStatus",
      "product_id": "productId",
    });
  }),
);

export function checkoutCreatedMetadataFromJSON(
  jsonString: string,
): SafeParseResult<CheckoutCreatedMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CheckoutCreatedMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CheckoutCreatedMetadata' from JSON`,
  );
}
