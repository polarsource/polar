/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type MeterCreditedMetadata = {
  meterId: string;
  units: number;
  rollover: boolean;
};

/** @internal */
export const MeterCreditedMetadata$inboundSchema: z.ZodMiniType<
  MeterCreditedMetadata,
  unknown
> = z.pipe(
  z.object({
    meter_id: z.string(),
    units: z.int(),
    rollover: z.boolean(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "meter_id": "meterId",
    });
  }),
);

export function meterCreditedMetadataFromJSON(
  jsonString: string,
): SafeParseResult<MeterCreditedMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MeterCreditedMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MeterCreditedMetadata' from JSON`,
  );
}
