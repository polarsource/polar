/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type MeterResetMetadata = {
  meterId: string;
};

/** @internal */
export const MeterResetMetadata$inboundSchema: z.ZodMiniType<
  MeterResetMetadata,
  unknown
> = z.pipe(
  z.object({
    meter_id: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "meter_id": "meterId",
    });
  }),
);

export function meterResetMetadataFromJSON(
  jsonString: string,
): SafeParseResult<MeterResetMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MeterResetMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MeterResetMetadata' from JSON`,
  );
}
