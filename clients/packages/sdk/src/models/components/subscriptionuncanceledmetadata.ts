/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type SubscriptionUncanceledMetadata = {
  subscriptionId: string;
  productId: string;
  amount: number;
  currency: string;
  recurringInterval: string;
  recurringIntervalCount: number;
};

/** @internal */
export const SubscriptionUncanceledMetadata$inboundSchema: z.ZodMiniType<
  SubscriptionUncanceledMetadata,
  unknown
> = z.pipe(
  z.object({
    subscription_id: z.string(),
    product_id: z.string(),
    amount: z.int(),
    currency: z.string(),
    recurring_interval: z.string(),
    recurring_interval_count: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "subscription_id": "subscriptionId",
      "product_id": "productId",
      "recurring_interval": "recurringInterval",
      "recurring_interval_count": "recurringIntervalCount",
    });
  }),
);

export function subscriptionUncanceledMetadataFromJSON(
  jsonString: string,
): SafeParseResult<SubscriptionUncanceledMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SubscriptionUncanceledMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SubscriptionUncanceledMetadata' from JSON`,
  );
}
