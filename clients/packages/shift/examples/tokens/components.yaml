button:
  $type: color
  background:
    $value: "{colors.primary}"
    $description: Button background — proxies the primary color design token
    $themes:
      dark: "{colors.secondary}"
  text:
    $value: "#ffffff"
    $description: Button text color
    $themes:
      dark: "#f0f0f0"

card:
  $type: color
  background:
    $value: "{colors.background}"
    $themes:
      dark: "#1a1a2e"
  border:
    $value: "#e5e7eb"
    $themes:
      dark: "#374151"
