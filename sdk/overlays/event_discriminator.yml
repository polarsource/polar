overlay: 1.0.0
info:
  title: Overlay to remove nested discriminator for Event schemas, which Speakeasy doesn't support
  version: 0.0.1
actions:
  - target: "$.components.schemas['Event'].discriminator"
    remove: true
