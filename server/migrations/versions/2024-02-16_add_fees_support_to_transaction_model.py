"""Add fees support to Transaction model

Revision ID: 0fdba225fb3d
Revises: 3c0a03756217
Create Date: 2024-02-16 10:56:12.142302

"""
import sqlalchemy as sa
from alembic import op

# Polar Custom Imports
from polar.kit.extensions.sqlalchemy import PostgresUUID

# revision identifiers, used by Alembic.
revision = "0fdba225fb3d"
down_revision = "3c0a03756217"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.create_table(
        "transaction_fee_settlements",
        sa.Column("fee_transaction_id", sa.UUID(), nullable=False),
        sa.Column("settlement_transaction_id", sa.UUID(), nullable=False),
        sa.Column("settlement_amount", sa.Integer(), nullable=False),
        sa.ForeignKeyConstraint(
            ["fee_transaction_id"],
            ["transactions.id"],
            name=op.f("transaction_fee_settlements_fee_transaction_id_fkey"),
            ondelete="cascade",
        ),
        sa.ForeignKeyConstraint(
            ["settlement_transaction_id"],
            ["transactions.id"],
            name=op.f("transaction_fee_settlements_settlement_transaction_id_fkey"),
            ondelete="cascade",
        ),
        sa.PrimaryKeyConstraint(
            "fee_transaction_id",
            "settlement_transaction_id",
            name=op.f("transaction_fee_settlements_pkey"),
        ),
    )
    op.add_column("transactions", sa.Column("fee_type", sa.String(), nullable=True))
    op.add_column(
        "transactions", sa.Column("to_settle_amount", sa.Integer(), nullable=True)
    )
    op.add_column(
        "transactions",
        sa.Column("incurred_by_transaction_id", sa.UUID(), nullable=True),
    )
    op.create_index(
        op.f("ix_transactions_fee_type"), "transactions", ["fee_type"], unique=False
    )
    op.create_index(
        op.f("ix_transactions_incurred_by_transaction_id"),
        "transactions",
        ["incurred_by_transaction_id"],
        unique=False,
    )
    op.create_index(
        op.f("ix_transactions_to_settle_amount"),
        "transactions",
        ["to_settle_amount"],
        unique=False,
    )
    op.create_foreign_key(
        op.f("transactions_incurred_by_transaction_id_fkey"),
        "transactions",
        "transactions",
        ["incurred_by_transaction_id"],
        ["id"],
        ondelete="set null",
    )

    # Create fee transactions before dropping the processor_fee_amount column
    op.execute(
        """
        INSERT INTO transactions (
            id,
            created_at,
            type,
            processor,
            currency,
            amount,
            account_currency,
            account_amount,
            tax_amount,
            fee_type,
            to_settle_amount,
            incurred_by_transaction_id,
            processor_fee_amount
        )
        SELECT
            uuid_generate_v4(),
            t.created_at,
            'fee',
            t.processor,
            t.currency,
            -t.processor_fee_amount,
            t.currency,
            -t.processor_fee_amount,
            0,
            CASE
                WHEN t.type = 'payment' THEN 'payment'
                WHEN t.type = 'dispute' THEN 'dispute'
                ELSE NULL
            END,
            0,
            t.id,
            0
        FROM transactions AS t
        WHERE processor_fee_amount > 0
        """
    )

    op.drop_column("transactions", "processor_fee_amount")
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "transactions",
        sa.Column(
            "processor_fee_amount", sa.INTEGER(), autoincrement=False, nullable=True
        ),
    )

    # Restore the processor_fee_amount column
    op.execute(
        """
        UPDATE transactions
        SET processor_fee_amount = -t.amount
        FROM transactions AS t
        WHERE t.incurred_by_transaction_id = transactions.id
        """
    )
    op.execute(
        """
        UPDATE transactions
        SET processor_fee_amount = 0
        WHERE processor_fee_amount IS NULL
        """
    )

    # Drop the fee transactions
    op.execute(
        """
        DELETE FROM transactions
        WHERE type = 'fee'
        """
    )

    op.alter_column(
        "transactions",
        "processor_fee_amount",
        nullable=False,
        existing_nullable=True,
        existing_type=sa.INTEGER(),
    )

    op.drop_constraint(
        op.f("transactions_incurred_by_transaction_id_fkey"),
        "transactions",
        type_="foreignkey",
    )
    op.drop_index(op.f("ix_transactions_to_settle_amount"), table_name="transactions")
    op.drop_index(
        op.f("ix_transactions_incurred_by_transaction_id"), table_name="transactions"
    )
    op.drop_index(op.f("ix_transactions_fee_type"), table_name="transactions")
    op.drop_column("transactions", "incurred_by_transaction_id")
    op.drop_column("transactions", "to_settle_amount")
    op.drop_column("transactions", "fee_type")
    op.drop_table("transaction_fee_settlements")
    # ### end Alembic commands ###
