"""Migrate individual webhook events flags to a JSON column

Revision ID: 8b001284e336
Revises: 974fd0c82d2e
Create Date: 2024-04-30 14:11:45.650502

"""

import sqlalchemy as sa
from alembic import op
from sqlalchemy.dialects import postgresql

# Polar Custom Imports
from polar.kit.extensions.sqlalchemy import PostgresUUID

# revision identifiers, used by Alembic.
revision = "8b001284e336"
down_revision = "974fd0c82d2e"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "webhook_endpoints",
        sa.Column("events", postgresql.JSONB(astext_type=sa.Text()), nullable=True),
    )

    # Migrate the data
    op.execute(
        """
        UPDATE webhook_endpoints
        SET events = array_to_json(
            array_remove(
                array[
                    CASE WHEN event_benefit_created THEN 'benefit.created' END,
                    CASE WHEN event_pledge_created THEN 'pledge.created' END,
                    CASE WHEN event_subscription_tier_created THEN 'subscription_tier.created' END,
                    CASE WHEN event_benefit_updated THEN 'benefit.updated' END,
                    CASE WHEN event_pledge_updated THEN 'pledge.updated' END,
                    CASE WHEN event_subscription_tier_updated THEN 'subscription_tier.updated' END,
                    CASE WHEN event_subscription_created THEN 'subscription.created' END,
                    CASE WHEN event_subscription_updated THEN 'subscription.updated' END,
                    CASE WHEN event_organization_updated THEN 'organization.updated' END,
                    CASE WHEN event_donation_created THEN 'donation.created' END
                ],
                NULL
            )
        )
        """
    )

    op.alter_column("webhook_endpoints", "events", nullable=False)

    op.drop_column("webhook_endpoints", "event_benefit_created")
    op.drop_column("webhook_endpoints", "event_pledge_created")
    op.drop_column("webhook_endpoints", "event_subscription_tier_created")
    op.drop_column("webhook_endpoints", "event_benefit_updated")
    op.drop_column("webhook_endpoints", "event_pledge_updated")
    op.drop_column("webhook_endpoints", "event_subscription_tier_updated")
    op.drop_column("webhook_endpoints", "event_subscription_created")
    op.drop_column("webhook_endpoints", "event_subscription_updated")
    op.drop_column("webhook_endpoints", "event_organization_updated")
    op.drop_column("webhook_endpoints", "event_donation_created")
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "webhook_endpoints",
        sa.Column(
            "event_donation_created",
            sa.BOOLEAN(),
            server_default=sa.text("false"),
            autoincrement=False,
            nullable=False,
        ),
    )
    op.add_column(
        "webhook_endpoints",
        sa.Column(
            "event_organization_updated",
            sa.BOOLEAN(),
            server_default=sa.text("false"),
            autoincrement=False,
            nullable=False,
        ),
    )
    op.add_column(
        "webhook_endpoints",
        sa.Column(
            "event_subscription_updated",
            sa.BOOLEAN(),
            server_default=sa.text("false"),
            autoincrement=False,
            nullable=False,
        ),
    )
    op.add_column(
        "webhook_endpoints",
        sa.Column(
            "event_subscription_created",
            sa.BOOLEAN(),
            server_default=sa.text("false"),
            autoincrement=False,
            nullable=False,
        ),
    )
    op.add_column(
        "webhook_endpoints",
        sa.Column(
            "event_subscription_tier_updated",
            sa.BOOLEAN(),
            server_default=sa.text("false"),
            autoincrement=False,
            nullable=False,
        ),
    )
    op.add_column(
        "webhook_endpoints",
        sa.Column(
            "event_pledge_updated",
            sa.BOOLEAN(),
            server_default=sa.text("false"),
            autoincrement=False,
            nullable=False,
        ),
    )
    op.add_column(
        "webhook_endpoints",
        sa.Column(
            "event_benefit_updated",
            sa.BOOLEAN(),
            server_default=sa.text("false"),
            autoincrement=False,
            nullable=False,
        ),
    )
    op.add_column(
        "webhook_endpoints",
        sa.Column(
            "event_subscription_tier_created",
            sa.BOOLEAN(),
            server_default=sa.text("false"),
            autoincrement=False,
            nullable=False,
        ),
    )
    op.add_column(
        "webhook_endpoints",
        sa.Column(
            "event_pledge_created",
            sa.BOOLEAN(),
            server_default=sa.text("false"),
            autoincrement=False,
            nullable=False,
        ),
    )
    op.add_column(
        "webhook_endpoints",
        sa.Column(
            "event_benefit_created",
            sa.BOOLEAN(),
            server_default=sa.text("false"),
            autoincrement=False,
            nullable=False,
        ),
    )

    # Migrate the data
    op.execute(
        """
        UPDATE webhook_endpoints
        SET event_benefit_created = events ? 'benefit.created',
            event_pledge_created = events ? 'pledge.created',
            event_subscription_tier_created = events ? 'subscription_tier.created',
            event_benefit_updated = events ? 'benefit.updated',
            event_pledge_updated = events ? 'pledge.updated',
            event_subscription_tier_updated = events ? 'subscription_tier.updated',
            event_subscription_created = events ? 'subscription.created',
            event_subscription_updated = events ? 'subscription.updated',
            event_organization_updated = events ? 'organization.updated',
            event_donation_created = events ? 'donation.created'
        """
    )

    op.drop_column("webhook_endpoints", "events")
    # ### end Alembic commands ###
