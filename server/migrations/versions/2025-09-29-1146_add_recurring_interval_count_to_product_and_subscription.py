"""Add recurring_interval_count to Product and Subscription models

Revision ID: bbdc673dc236
Revises: 1ec8eb5dcb44
Create Date: 2025-09-29 11:46:22.707113

"""

import sqlalchemy as sa
from alembic import op

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "bbdc673dc236"
down_revision = "1ec8eb5dcb44"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "products",
        sa.Column("recurring_interval_count", sa.Integer(), nullable=True),
    )
    op.add_column(
        "subscriptions",
        sa.Column(
            "recurring_interval_count",
            sa.Integer(),
            nullable=False,
            server_default="0",
        ),
    )

    # Set recurring_interval_count = 1 for existing records with recurring_interval set
    op.execute(
        "UPDATE products SET recurring_interval_count = 1 WHERE recurring_interval IS NOT NULL"
    )
    op.execute(
        "UPDATE subscriptions SET recurring_interval_count = 1 WHERE recurring_interval IS NOT NULL"
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column("subscriptions", "recurring_interval_count")
    op.drop_column("products", "recurring_interval_count")
    # ### end Alembic commands ###
