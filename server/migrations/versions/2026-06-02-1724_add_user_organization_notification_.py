"""add user_organization.notification_settings

Revision ID: 4c80966d7217
Revises: d2a49dc19a62
Create Date: 2026-06-02 17:24:26.410316

"""

import sqlalchemy as sa
from alembic import op
from sqlalchemy.dialects import postgresql

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "4c80966d7217"
down_revision = "d2a49dc19a62"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration
    op.execute("SET LOCAL lock_timeout = '5s'")
    # Note: Backfill from organisation-level to user-level notification settings will be applied in a follow-up migration
    # to avoid deployment drift when the application code that reads these columns is merged separately.
    op.add_column(
        "user_organizations",
        sa.Column(
            "notification_settings",
            postgresql.JSONB(astext_type=sa.Text()),
            nullable=True,
        ),
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column("user_organizations", "notification_settings")
    # ### end Alembic commands ###
