"""add assigned_user_id to support_cases

Revision ID: e70dad60eade
Revises: aa6ab151feda
Create Date: 2026-06-10 15:13:11.339680

"""

import sqlalchemy as sa
from alembic import op

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "e70dad60eade"
down_revision = "aa6ab151feda"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "support_cases", sa.Column("assigned_user_id", sa.Uuid(), nullable=True)
    )
    op.create_index(
        op.f("ix_support_cases_assigned_user_id"),
        "support_cases",
        ["assigned_user_id"],
        unique=False,
    )
    op.create_foreign_key(
        op.f("support_cases_assigned_user_id_fkey"),
        "support_cases",
        "users",
        ["assigned_user_id"],
        ["id"],
        ondelete="set null",
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_constraint(
        op.f("support_cases_assigned_user_id_fkey"), "support_cases", type_="foreignkey"
    )
    op.drop_index(op.f("ix_support_cases_assigned_user_id"), table_name="support_cases")
    op.drop_column("support_cases", "assigned_user_id")
    # ### end Alembic commands ###
