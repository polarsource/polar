"""add sso_enforced to organization

Revision ID: 6cd2cda5a8a0
Revises: f159733f3ae6
Create Date: 2026-07-01 14:18:53.080094

"""

import sqlalchemy as sa
from alembic import op

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "6cd2cda5a8a0"
down_revision = "42886ceacac8"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration
    op.execute("SET LOCAL lock_timeout = '5s'")
    op.add_column(
        "organizations",
        sa.Column("sso_enforced", sa.Boolean(), nullable=True),
    )
    op.execute("UPDATE organizations SET sso_enforced = false")
    op.alter_column(
        "organizations", "sso_enforced", existing_type=sa.Boolean(), nullable=False
    )


def downgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column("organizations", "sso_enforced")
    # ### end Alembic commands ###
