"""add embed_hosts to organizations

Revision ID: b493ccc15e3f
Revises: d3f5a9c47e21
Create Date: 2026-07-28 15:47:48.588911

"""

import sqlalchemy as sa
from alembic import op
from sqlalchemy.dialects import postgresql

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "b493ccc15e3f"
down_revision = "d3f5a9c47e21"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration
    # `organizations` is a busy table: allow longer to acquire the lock.
    op.execute("SET LOCAL lock_timeout = '30s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "organizations",
        sa.Column(
            "embed_hosts", postgresql.JSONB(astext_type=sa.Text()), nullable=True
        ),
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration
    # `organizations` is a busy table: allow longer to acquire the lock.
    op.execute("SET LOCAL lock_timeout = '30s'")
    op.drop_column("organizations", "embed_hosts")
