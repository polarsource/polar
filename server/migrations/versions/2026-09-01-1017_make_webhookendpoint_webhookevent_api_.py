"""Make WebhookEndpoint/WebhookEvent.api_version non nullable

Revision ID: fec1e6242c3f
Revises: b58196b41f41
Create Date: 2026-09-01 10:17:43.303341

"""

import sqlalchemy as sa
from alembic import op

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "fec1e6242c3f"
down_revision = "b58196b41f41"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration
    op.execute("SET LOCAL lock_timeout = '5s'")

    op.execute(
        """
        UPDATE webhook_endpoints
        SET api_version = '2026-04'
        WHERE api_version IS NULL
        """
    )
    op.execute(
        """
        UPDATE webhook_events
        SET api_version = '2026-04'
        WHERE api_version IS NULL
        """
    )

    op.alter_column(
        "webhook_endpoints",
        "api_version",
        existing_type=sa.CHAR(length=7),
        nullable=False,
    )
    op.alter_column(
        "webhook_events", "api_version", existing_type=sa.CHAR(length=7), nullable=False
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.alter_column(
        "webhook_events", "api_version", existing_type=sa.CHAR(length=7), nullable=True
    )
    op.alter_column(
        "webhook_endpoints",
        "api_version",
        existing_type=sa.CHAR(length=7),
        nullable=True,
    )
    # ### end Alembic commands ###
