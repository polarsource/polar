"""add customer_session_codes.code_v2

Revision ID: e56779e4ad36
Revises: 6259a4ed4d4b
Create Date: 2026-09-17 16:59:07.250782

"""

import sqlalchemy as sa
from alembic import op

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "e56779e4ad36"
down_revision = "6259a4ed4d4b"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "customer_session_codes",
        sa.Column("code_v2", sa.String(length=80), nullable=True),
    )
    op.create_unique_constraint(
        op.f("customer_session_codes_code_v2_key"),
        "customer_session_codes",
        ["code_v2"],
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_constraint(
        op.f("customer_session_codes_code_v2_key"),
        "customer_session_codes",
        type_="unique",
    )
    op.drop_column("customer_session_codes", "code_v2")
    # ### end Alembic commands ###
