"""add oauth2_clients.registration_access_token_hash_v2

Revision ID: 840a72ba0ea4
Revises: dded653fed1f
Create Date: 2026-09-17 16:59:28.763639

"""

import sqlalchemy as sa
from alembic import op

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "840a72ba0ea4"
down_revision = "dded653fed1f"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "oauth2_clients",
        sa.Column(
            "registration_access_token_hash_v2", sa.String(length=80), nullable=True
        ),
    )
    op.create_index(
        op.f("ix_oauth2_clients_registration_access_token_hash_v2"),
        "oauth2_clients",
        ["registration_access_token_hash_v2"],
        unique=False,
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_index(
        op.f("ix_oauth2_clients_registration_access_token_hash_v2"),
        table_name="oauth2_clients",
    )
    op.drop_column("oauth2_clients", "registration_access_token_hash_v2")
    # ### end Alembic commands ###
