"""add personal_access_tokens.token_v2

Revision ID: ac945e3bc7e8
Revises: e56779e4ad36
Create Date: 2026-09-17 16:59:10.689966

"""

import sqlalchemy as sa
from alembic import op

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "ac945e3bc7e8"
down_revision = "e56779e4ad36"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "personal_access_tokens",
        sa.Column("token_v2", sa.String(length=80), nullable=True),
    )
    op.create_unique_constraint(
        op.f("personal_access_tokens_token_v2_key"),
        "personal_access_tokens",
        ["token_v2"],
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_constraint(
        op.f("personal_access_tokens_token_v2_key"),
        "personal_access_tokens",
        type_="unique",
    )
    op.drop_column("personal_access_tokens", "token_v2")
    # ### end Alembic commands ###
