"""make user_sessions.token nullable

Revision ID: 256bcd7ce266
Revises: 840a72ba0ea4
Create Date: 2026-09-17 18:15:51.488093

"""

import sqlalchemy as sa
from alembic import op

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "256bcd7ce266"
down_revision = "840a72ba0ea4"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.alter_column(
        "user_sessions", "token", existing_type=sa.CHAR(length=64), nullable=True
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.alter_column(
        "user_sessions", "token", existing_type=sa.CHAR(length=64), nullable=False
    )
    # ### end Alembic commands ###
