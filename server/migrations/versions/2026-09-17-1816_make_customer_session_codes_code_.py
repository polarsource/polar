"""make customer_session_codes.code nullable

Revision ID: 980256d1a467
Revises: 5e0468b8fde5
Create Date: 2026-09-17 18:16:01.077516

"""

import sqlalchemy as sa
from alembic import op

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "980256d1a467"
down_revision = "5e0468b8fde5"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.alter_column(
        "customer_session_codes",
        "code",
        existing_type=sa.CHAR(length=64),
        nullable=True,
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.alter_column(
        "customer_session_codes",
        "code",
        existing_type=sa.CHAR(length=64),
        nullable=False,
    )
    # ### end Alembic commands ###
