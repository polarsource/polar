"""make personal_access_tokens.token nullable

Revision ID: add48e2c0ea4
Revises: 980256d1a467
Create Date: 2026-09-17 18:16:04.141259

"""

import sqlalchemy as sa
from alembic import op

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "add48e2c0ea4"
down_revision = "980256d1a467"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.alter_column(
        "personal_access_tokens",
        "token",
        existing_type=sa.CHAR(length=64),
        nullable=True,
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.alter_column(
        "personal_access_tokens",
        "token",
        existing_type=sa.CHAR(length=64),
        nullable=False,
    )
    # ### end Alembic commands ###
