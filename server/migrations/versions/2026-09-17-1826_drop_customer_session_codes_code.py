"""drop customer_session_codes.code

Revision ID: 24bbaa979785
Revises: b2a2d9433961
Create Date: 2026-09-17 18:26:50.045587

"""

import sqlalchemy as sa
from alembic import op

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "24bbaa979785"
down_revision = "b2a2d9433961"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_constraint(
        op.f("customer_session_codes_code_key"),
        "customer_session_codes",
        type_="unique",
    )
    op.drop_column("customer_session_codes", "code")
    # ### end Alembic commands ###


def downgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "customer_session_codes",
        sa.Column("code", sa.CHAR(length=64), autoincrement=False, nullable=True),
    )
    op.create_unique_constraint(
        op.f("customer_session_codes_code_key"),
        "customer_session_codes",
        ["code"],
        postgresql_nulls_not_distinct=False,
    )
    # ### end Alembic commands ###
