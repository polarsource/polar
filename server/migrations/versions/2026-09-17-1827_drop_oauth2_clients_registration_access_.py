"""drop oauth2_clients.registration_access_token_hash

Revision ID: f9d1169d7b94
Revises: 7d940d46c8c5
Create Date: 2026-09-17 18:27:02.910796

"""

import sqlalchemy as sa
from alembic import op

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "f9d1169d7b94"
down_revision = "7d940d46c8c5"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_index(
        op.f("ix_oauth2_clients_registration_access_token_hash"),
        table_name="oauth2_clients",
    )
    op.drop_column("oauth2_clients", "registration_access_token_hash")
    # ### end Alembic commands ###


def downgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "oauth2_clients",
        sa.Column(
            "registration_access_token_hash",
            sa.VARCHAR(length=64),
            autoincrement=False,
            nullable=True,
        ),
    )
    op.create_index(
        op.f("ix_oauth2_clients_registration_access_token_hash"),
        "oauth2_clients",
        ["registration_access_token_hash"],
        unique=False,
    )
    # ### end Alembic commands ###
