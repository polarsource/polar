"""drop the oauth2 client plaintext secrets

Revision ID: 3174cde85cb5
Revises: 125721f7f17d
Create Date: 2026-09-21 18:23:44.622594

"""

import sqlalchemy as sa
from alembic import op

# Polar Custom Imports

# revision identifiers, used by Alembic.
revision = "3174cde85cb5"
down_revision = "125721f7f17d"
branch_labels: tuple[str] | None = None
depends_on: tuple[str] | None = None


def upgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_index(
        op.f("ix_oauth2_clients_registration_access_token"), table_name="oauth2_clients"
    )
    op.drop_column("oauth2_clients", "registration_access_token")
    op.drop_column("oauth2_clients", "client_secret")
    # ### end Alembic commands ###


def downgrade() -> None:
    # Ensures we don't break app by applying a deadlock-inducing migration.
    # CREATE INDEX CONCURRENTLY needs its own, far larger timeout -- see ADR-0006.
    op.execute("SET LOCAL lock_timeout = '5s'")
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "oauth2_clients",
        sa.Column(
            "client_secret", sa.VARCHAR(length=52), autoincrement=False, nullable=True
        ),
    )
    op.add_column(
        "oauth2_clients",
        sa.Column(
            "registration_access_token",
            sa.VARCHAR(),
            autoincrement=False,
            nullable=True,
        ),
    )
    op.create_index(
        op.f("ix_oauth2_clients_registration_access_token"),
        "oauth2_clients",
        ["registration_access_token"],
        unique=False,
    )
    # ### end Alembic commands ###
