NODE materialize
SQL >
    SELECT organization_id, toStartOfHour(timestamp) AS hour, customer_id, external_customer_id
    FROM events_by_ingested_at
    GROUP BY organization_id, hour, customer_id, external_customer_id

TYPE MATERIALIZED
DATASOURCE user_events_by_hour
