NODE materialize
SQL >
    SELECT
        organization_id,
        toStartOfFifteenMinutes(timestamp) AS quarter_hour,
        customer_id,
        external_customer_id
    FROM events_by_ingested_at
    GROUP BY organization_id, quarter_hour, customer_id, external_customer_id

TYPE MATERIALIZED
DATASOURCE user_events_by_quarter_hour
